id: etc-gshadow-permissions
title: Ensure permissions on /etc/gshadow are configured
description: >
  The /etc/gshadow file must have correct ownership and permissions.
rationale: >
  Incorrect permissions on /etc/gshadow could allow unauthorized access or modification of sensitive system data.
severity: high
category: filesystem
tags: [file-permissions, authentication]

references:
  cis:
    rhel9_v2: { section: "7.1.7", level: "L1", type: "Automated" }
  nist_800_53: ["AC-3", "MP-2"]

platforms:
  - family: rhel
    min_version: 8
implementations:
  - default: true
    check:
      method: file_permission
      path: "/etc/gshadow"
      owner: "root"
      group: "root"
      mode: "0000"
    remediation:
      mechanism: file_permissions
      path: "/etc/gshadow"
      owner: "root"
      group: "root"
      mode: "0000"
