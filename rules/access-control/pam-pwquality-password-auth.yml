id: pam-pwquality-password-auth
title: Enable pam_pwquality module in /etc/pam.d/password-auth
description: >
  The pam_pwquality.so module must be enabled in /etc/pam.d/password-auth
  to enforce password complexity requirements during password changes.
rationale: >
  Without pam_pwquality in the password-auth PAM configuration, password
  complexity rules will not be enforced for authentication paths that use
  password-auth, allowing weak passwords to be set.
severity: medium
category: access-control
tags: [pam, pwquality, password-complexity, password-auth]

references:
  stig:
    rhel9_v2r7:
      vuln_id: "V-258097"
      stig_id: "RHEL-09-611025"
      severity: "CAT II"
  nist_800_53: ["IA-5(1)"]

platforms:
  - family: rhel
    min_version: 8

implementations:
  - when: authselect
    check:
      method: command
      run: 'authselect current 2>/dev/null | grep -q with-pwquality'
      expected_exit: 0
    remediation:
      mechanism: command_exec
      run: "authselect enable-feature with-pwquality"
  - default: true
    check:
      method: command
      run: >
        grep -qE '^\s*password\s+.*pam_pwquality\.so'
        /etc/pam.d/password-auth 2>/dev/null
      expected_exit: 0
    remediation:
      mechanism: manual
      note: >
        Add pam_pwquality.so to the password stack in
        /etc/pam.d/password-auth. Preferred: authselect enable-feature with-pwquality.
