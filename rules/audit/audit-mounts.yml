id: audit-mounts
title: Ensure successful file system mounts are collected
description: >
  Successful file system mounts must be collected.
rationale: >
  Without auditing these events, malicious activity could go undetected, compromising forensic capabilities.
severity: medium
category: audit
tags: [audit, auditd]

supersedes: [audit-mount-operations]
depends_on: [auditd-service-enabled]

references:
  cis:
    rhel9: { section: "6.3.3.10", level: "L1", type: "Automated" }
  stig:
    rhel9:
      vuln_id: "V-257806"
      stig_id: "RHEL-09-654035"
      severity: "CAT II"
  nist_800_53: ["AU-2", "AU-12"]

platforms:
  - family: rhel
    min_version: 8
implementations:
  - default: true
    check:
      method: audit_rule_exists
      rule: "-k mounts"
    remediation:
      mechanism: audit_rule_set
      rule: "-a always,exit -F arch=b64 -S mount -F auid>=1000 -F auid!=unset -k mounts"
      persist_file: "/etc/audit/rules.d/50-mounts.rules"
